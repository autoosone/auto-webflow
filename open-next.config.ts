import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Configuration options for OpenNext on Cloudflare
  // This will work with Webflow Cloud's infrastructure
});